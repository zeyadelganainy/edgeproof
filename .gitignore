node_modules/
dist/
build/
*.log
.DS_Store
# Local language-probe scratch
scratch/
# CLI runtime logs + local private-state DB
cli/logs/
cli/.midnight-level-db/
# Vercel deploy metadata
.vercel/
# Local secrets (testnet wallet seeds) — never commit
.secrets/
# Notes:
# - TS build output (dist/) is regenerated by `npm run build`.
# - The compiled Compact circuit assets under contract/src/managed/ ARE committed
#   on purpose, so the repo builds + runs without the Compact toolchain installed.
# - data/ is committed — the demo datasets are deliverables.
